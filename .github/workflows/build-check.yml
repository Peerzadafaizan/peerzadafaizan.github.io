name: Build check

# Type-checks, builds, verifies and content-parity-checks the React rebuild. Build-only: this workflow never deploys.
# GitHub Pages keeps deploying `main` exactly as before until a separate, approved cutover.
on:
  push:
    branches: [redesign/react]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history: the content-parity check reads the original page from main @ bd1dab3.
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install (from lockfile)
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Verify SEO contract, CSP-safe markup and public/ assets
        run: npm run verify

      - name: Content parity vs main @ bd1dab3 (text, headings, links, metadata, JSON-LD)
        run: npm run parity

      - name: Upload build output (for review only — not deployed)
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          include-hidden-files: true
          retention-days: 7
