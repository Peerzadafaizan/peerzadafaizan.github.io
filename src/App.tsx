/**
 * Stage 1 scaffold placeholder.
 * Portfolio sections are NOT built yet — they are ported from the existing site, word for word,
 * in later stages. This component only proves the React + TypeScript + Tailwind pipeline and tokens.
 */
export default function App() {
  return (
    <main id="main" className="mx-auto max-w-content px-gutter py-section">
      <p className="font-mono text-label font-semibold uppercase tracking-[0.1em] text-accent">
        React rebuild · Stage 1 scaffold
      </p>
      <h1 className="mt-3 font-display text-display font-semibold tracking-[-0.015em] text-ink">
        Peerzada Faizan Ahmad
      </h1>
      <p className="mt-4 max-w-measure text-ink-body">
        This branch is being rebuilt in React. Portfolio sections are added in later stages.
      </p>
    </main>
  )
}
